# 명령어 구조 점검 보고서

## 📋 점검 일자
2025-01-27

## ✅ 복구 완료

### 1. 신고 기능 복구
- **위치**: `server/labbot-node.js`의 `handleMessage()` 함수 시작 부분
- **처리 순서**: 명령어 처리 전에 최우선 처리
- **조건**: `replyToMessageId`가 있고 메시지가 "!신고"로 시작
- **기능**: `chatLogger.saveReport()` 호출하여 신고 저장

## 📝 현재 지원 명령어

### 모듈화된 명령어 (`server/bot/commands/`)
1. **!질문** - 네이버 카페 질문 작성
   - 파일: `server/bot/commands/cafe/questionCommand.js`
   
2. **!뉴스** - 네이버 뉴스 검색
   - 파일: `server/bot/commands/news/newsCommand.js`
   
3. **!통계** - 채팅 통계 조회
   - 파일: `server/bot/commands/user/statsCommand.js`
   
4. **!이미지** - 이미지 업로드 안내
   - 파일: `server/bot/commands/user/imageCommand.js`
   
5. **!hi** - 인사 및 도움말
   - 파일: `server/bot/commands/user/hiCommand.js`

### 특수 기능 (명령어가 아닌 기능)
- **신고 기능** - 답장 버튼 + "!신고" (최우선 처리)

## ⚠️ 확인 필요 사항

### 포인트/랭킹/상점/공지 명령어
- **상태**: 함수는 존재하지만 명령어 처리 로직이 없음
- **함수 존재 여부**:
  - ✅ `addPoints()` - `server/labbot-node.js` (97줄)
  - ✅ `reducePoints()` - `server/labbot-node.js` (133줄)
  - ✅ `getChatRankings()` - `server/bot/commands/user/statsService.js` (모듈화됨)
  - ✅ `registerItem()` - `server/labbot-node.js` (211줄)
  - ✅ `removeItem()` - `server/labbot-node.js` (225줄)
  - ✅ `NOTICE_SYSTEM` - `server/bot/systems/noticeSystem.js` (모듈화됨)

- **문제**: 명령어 라우터(`server/bot/commands/index.js`)에 해당 명령어 처리 로직이 없음

## 🔍 리팩토링 전후 비교

### 리팩토링 전 (추정)
- `handleMessage()` 함수 내에서 직접 명령어 처리
- 포인트, 랭킹, 상점, 공지 명령어가 `handleMessage()` 내부에 있었을 것으로 추정

### 리팩토링 후 (현재)
- 명령어 처리가 `handleCommand()` 모듈로 분리
- 일부 명령어만 모듈화됨
- 포인트/랭킹/상점/공지 명령어 처리 로직 누락 가능성

## 📌 다음 단계

1. **리팩토링 전 코드 확인**
   - Git 커밋 히스토리에서 리팩토링 전 `handleMessage()` 함수 확인
   - 포인트/랭킹/상점/공지 명령어 처리 로직 확인

2. **누락된 명령어 복구**
   - 포인트 명령어 처리 추가
   - 랭킹 명령어 처리 추가
   - 상점 명령어 처리 추가
   - 공지 명령어 처리 추가

3. **명령어 모듈화 (선택사항)**
   - 포인트/랭킹/상점/공지 명령어도 모듈로 분리
   - 또는 `handleCommand()` 모듈에 직접 추가

## 🔧 수정 사항

### `server/labbot-node.js`
- ✅ 신고 기능 처리 로직 추가 (275-365줄)

### `server/bot/commands/index.js`
- ⚠️ 포인트/랭킹/상점/공지 명령어 처리 로직 추가 필요

## 📚 참고 문서

- `docs/features/REPORT_FEATURE_DOCUMENTATION.md` - 신고 기능 상세 문서
- `server/IMPLEMENTATION_SUMMARY.md` - 구현 요약
- `REFACTORING_COMPLETE_REPORT.md` - 리팩토링 완료 보고서

